同node的直接转发，如果node不同，则通过路由表转发.
每台机器只能连接一个外节点
node通过表路由，virtualIp直接路由.
如果是1M接入，可以有50个接入点

COMM通过nodeId可以找到转发的Ip

COMM收到后，判断nodeId是否相同，如果不同，则是需要转发的，一个vip只转发一个node的数据
所以每个COMM需要有一个节点转发表。
如果收到非本节点能转发的数据包后，则丢弃.